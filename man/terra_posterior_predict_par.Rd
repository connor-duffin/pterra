% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pterra.R
\name{terra_posterior_predict_par}
\alias{terra_posterior_predict_par}
\title{Parallel simulation from the posterior predictive distribution with {terra}.}
\usage{
terra_posterior_predict_par(
  file_raster,
  file_out,
  model,
  draws = 1,
  cores = 1,
  ...
)
}
\arguments{
\item{file_raster}{Path to the input raster file (readable by
\code{\link[terra:rast]{terra::rast()}}). This file provides the covariate data for prediction.}

\item{file_out}{Path to the output raster file where posterior predictive
draws will be written.}

\item{model}{A fitted Stan model object, typically returned from a {cmdstanr}
modeling workflow.}

\item{draws}{Integer specifying the total number of posterior draws to
simulate. These are split approximately evenly across cores.}

\item{cores}{Integer number of parallel workers to use.}

\item{...}{Additional arguments passed to \code{\link[terra:predict]{terra::predict()}} via
\code{\link[=terra_posterior_predict]{terra_posterior_predict()}}.}
}
\value{
A string giving the file path to the combined output raster.
}
\description{
Reads in a raster file, performs posterior predictive simulations in
parallel, and writes the resulting combined raster to disk. This function is
useful for large-scale raster predictions where computation can be split
across multiple cores.
}
\details{
Each core handles a subset of the posterior draws and writes results to a
temporary file, which are then combined and written to the specified output.
}
